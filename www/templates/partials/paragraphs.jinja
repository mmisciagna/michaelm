{% for p in section.paragraphs %}
  {% if p.subtitle %}
    <h4 class="section__subtitle">
      {{ p.subtitle }}
    </h4>
  {% endif %}
  <p class="section__paragraph">
    {{ p.text|safe }}
  </p>
{% endfor %}
