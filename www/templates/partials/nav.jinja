<nav class="nav"
    aria-label="Navigation">
  <ul class="nav__list"
      role="menubar"
      aria-label="Navigation menubar">
    {% for item in nav|sort(attribute='order') %}
      <li class="nav__list-item"
          role="none">
        {% set isActive = item.label|lower == content.title|lower %}
        <a class="nav__link {% if isActive %}nav__link--active{% endif %}"
            href="{{ item.path }}"
            role="menuitem">
          {{ item.label|capitalize }}
        </a>
      </li>
    {% endfor %}
  </ul>
</nav>
