interface RouteDetails {
  element: React.ReactElement;
  label?: string;
  path: string;
  showcasePortal?: boolean;
}

interface Showcase {
  description: string;
  client: string;
  img: string;
  role: string;
  title: string;
}
